import React from 'react';
import { Content, SnackClose, Text, Card } from './styles';

const Snackbar = ( ) => {
  return (
    <Content>
      <SnackClose />
        <Text>
          OLA QUE TAL
        </Text>
      <Card>
        
      </Card>
    </Content>
  )
}

export default Snackbar;